import { React, CodeBlock, monokai } from '../../../../deps.ts';

const Persistence = (props: any) => {

  return (
    <div className="docContainer">
      <h1>Persistence</h1>
    </div>
  )
}

export default Persistence;
