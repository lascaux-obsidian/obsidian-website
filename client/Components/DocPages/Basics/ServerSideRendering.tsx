import { React, CodeBlock, monokai } from '../../../../deps.ts';

const ServerSideRendering = (props: any) => {

  return (
    <div className="docContainer">
      <h1>Server-Side Rendering</h1>
      <p><code className="obsidianInline">obsidian</code> was built with the goal of bringing a powerful GraphQL server and client caching implementation to Deno, and as such it is designed to work with React apps served with server-side rendering, tightly coupling <code className="obsidianInline">obsidian</code> to the way applications are built today.</p>
    </div>
  )
}

export default ServerSideRendering;
